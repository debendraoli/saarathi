org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
# On AGP 9.3.0 / Flutter 3.47.2: `flutter_tts` was the only dependency still
# applying the legacy `kotlin-android` plugin unconditionally, which
# conflicts with built-in Kotlin — removed (see turn_announcer.dart's
# removal). firebase_core/flutter_webrtc/location/mobile_scanner/maplibre_gl
# all correctly check the real `android.builtInKotlin` property at their
# current versions, so this is safe to enable now. If a future dependency
# bump or new plugin reintroduces an unconditional `apply plugin:
# 'kotlin-android'`, flip this back to `false`.
android.builtInKotlin=true
